RSpec.describe Simple::Thread::Exercise do
  it "has a version number" do
    expect(Simple::Thread::Exercise::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
